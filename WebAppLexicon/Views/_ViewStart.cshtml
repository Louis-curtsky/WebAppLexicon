@{
    if (Context.User.Identity.IsAuthenticated && (User.IsInRole("Admin") || User.IsInRole("SuperAdmin")))
    {
        Layout = "_Layout2";
    }
    else
    {
        Layout = "_Layout";
    }
}
